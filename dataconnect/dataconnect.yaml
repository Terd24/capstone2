specVersion: "v1"
serviceId: "student-login-pwa"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "student-login-pwa-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
