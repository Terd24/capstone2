🚨 ULTIMATE FIX - Missing Columns in student_fee_items
======================================================

PROBLEM:
-------
The student_fee_items table is missing multiple columns:
- student_name
- grade_level  
- date_added

This is why balance creation keeps failing!

SOLUTION (Choose ONE):
---------------------

🔥 OPTION 1: Automated Fix (RECOMMENDED - 30 seconds)
----------------------------------------------------
1. Open browser: http://localhost/onecci/fix_complete_table.php

2. This script will:
   ✅ Check which columns are missing
   ✅ Add ALL missing columns automatically
   ✅ Create a test balance
   ✅ Show you the results

3. Refresh Cashier page
4. Balance should appear!

⚡ OPTION 2: Manual SQL Fix (1 minute)
-------------------------------------
1. Open phpMyAdmin
2. Select onecci_db database
3. Click SQL tab
4. Copy and paste this ENTIRE script:

-- Add missing columns
ALTER TABLE student_fee_items ADD COLUMN student_name VARCHAR(200) AFTER id_number;
ALTER TABLE student_fee_items ADD COLUMN grade_level VARCHAR(50) AFTER student_name;
ALTER TABLE student_fee_items ADD COLUMN date_added TIMESTAMP DEFAULT CURRENT_TIMESTAMP;

-- Verify columns were added
DESCRIBE student_fee_items;

-- Create test balance
INSERT INTO student_fee_items 
(id_number, student_name, grade_level, school_year_term, fee_type, amount, paid)
VALUES 
('02200000001', 'Gesterd Go', 'Grade 12', '2025-2026 1st Semester', 'Document Request Fee - Form137', 50.00, 0);

-- Verify it was created
SELECT * FROM student_fee_items WHERE id_number = '02200000001' ORDER BY id DESC LIMIT 1;

5. Click "Go" to execute
6. Refresh Cashier page
7. Should see the balance!

NOTE: If you get "Duplicate column name" errors, that's OK! 
It means those columns already exist. Just continue with the INSERT statement.

🎯 OPTION 3: Recreate Table (Nuclear option - 2 minutes)
-------------------------------------------------------
If adding columns doesn't work, recreate the entire table:

-- Backup existing data (if any)
CREATE TABLE student_fee_items_backup AS SELECT * FROM student_fee_items;

-- Drop and recreate table
DROP TABLE student_fee_items;

CREATE TABLE student_fee_items (
    id INT AUTO_INCREMENT PRIMARY KEY,
    id_number VARCHAR(50),
    student_name VARCHAR(200),
    grade_level VARCHAR(50),
    school_year_term VARCHAR(50),
    fee_type VARCHAR(200),
    amount DECIMAL(10,2),
    paid DECIMAL(10,2) DEFAULT 0,
    date_added TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_student (id_number),
    INDEX idx_term (school_year_term)
);

-- Restore data (if any existed)
INSERT INTO student_fee_items SELECT * FROM student_fee_items_backup;

-- Create test balance
INSERT INTO student_fee_items 
(id_number, student_name, grade_level, school_year_term, fee_type, amount, paid)
VALUES 
('02200000001', 'Gesterd Go', 'Grade 12', '2025-2026 1st Semester', 'Document Request Fee - Form137', 50.00, 0);

VERIFICATION:
------------
After running ANY option above:

1. Open Cashier system
2. Search for: "ges" or "02200000001"
3. Should see: "Document Request Fee - Form137: ₱50.00"
4. Try processing payment
5. If it works → SUCCESS! ✅

AFTER FIX:
---------
Once the table is fixed:
1. Automatic balance creation will work
2. When Registrar approves document request
3. Balance will appear in Cashier automatically
4. No more manual fixes needed!

TEST THE SYSTEM:
---------------
1. Set document fee in Owner Dashboard (e.g., Form137 = ₱50)
2. Student requests Form137
3. Registrar approves request
4. Check Cashier → Balance should appear automatically!

TROUBLESHOOTING:
---------------
If balance still doesn't appear after fix:

1. Check PHP error log: C:\xampp\php\logs\php_error_log
2. Look for lines with "CREATE DOCUMENT BALANCE"
3. Run: http://localhost/onecci/debug_document_balance.php
4. Check what it shows

COMMON ISSUES:
-------------
❌ "Duplicate column name" → Column already exists, skip that ALTER
❌ "Table doesn't exist" → Create table using Option 3
❌ "Access denied" → Login to phpMyAdmin as root
❌ Balance still not showing → Check document name matches exactly

NEED IMMEDIATE HELP?
-------------------
If you're stuck:
1. Run Option 1 (fix_complete_table.php)
2. Screenshot the output
3. Check what errors it shows
4. Try Option 2 if Option 1 fails

THIS WILL FIX IT! 💪
The table structure is the root cause. Once fixed, everything will work!
